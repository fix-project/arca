cargo-features = ["per-package-target"]

[package]
name = "fixutils"
version = "0.1.0"
edition = "2024"
forced-target = "wasm32-unknown-unknown"

[lib]
crate-type = ["rlib"]

[dependencies]
macros = { path = "../../macros" }