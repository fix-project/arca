[package]
name = "fixutils"
version = "0.1.0"
edition = "2024"

[lib]
crate-type = ["rlib"]

[dependencies]
fixhandle = { path = "../handle", default-features = false }
macros = { path = "../../macros" }

[build-dependencies]
cc = "1.2.30"