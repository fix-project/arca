a = create_blob(Int(2)); 
b = create_blob(Int(3)); 
c = create_blob(Path("../target/x86_64-unknown-none/addblob"));
d = create_tree(c, a, b); 
e = create_blob(Int(1)); 
f = create_tree(c, e, apply(d));
g = apply(f); 
print(get_blob(apply(f)));
