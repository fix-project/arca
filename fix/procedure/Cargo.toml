[package]
name = "fixprocedure"
version = "0.1.0"
edition = "2024"

[lib]
crate-type = ["cdylib"]

[dependencies]
fixutils = { path = "../utils" }
dlmalloc = { version = "0.2.14", features = ["global"] }