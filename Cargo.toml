[workspace]
members = [ "common", "vmm", "kernel", "macros", "xtask"]
default-members = ["kernel", "xtask"]
resolver = "2"

[profile.dev]
panic = "abort"

[profile.release]
panic = "abort"
debug = true
